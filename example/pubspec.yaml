name: example
description: flutter_html example app.
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter_html:
    path: ..
  flutter:
    sdk: flutter
  wakelock: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/html5.png
    - assets/mac.svg
